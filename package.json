{
    "name": "hyperblast",
    "version": "1.0.0",
    "description": "Lazer Tag application with React frontend and Node.js backend",
    "main": "index.js",
    "scripts": {
        "start": "node start-all.js",
        "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install wait-on",
        "build": "npm run build --prefix frontend && npm run build --prefix backend"
    },
    "keywords": [
        "lazer-tag",
        "react",
        "node",
        "tensorflow"
    ],
    "author": "Group 5",
    "license": "ISC",
    "devDependencies": {
        "concurrently": "^8.2.0",
        "devcert": "^1.2.2",
        "mkcert": "^3.2.0"
    },
    "dependencies": {
        "wait-on": "^8.0.4"
    }
}
